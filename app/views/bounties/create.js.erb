$('#content').prepend(
  '<%= j render 'shared/flash', flash: tr(:bounty, 'created', 'female') %>'
);
$('<%= j render 'bounties/bounty', bounty: resource %>').insertAfter('.comments');
$('#new_bounty').hide();
