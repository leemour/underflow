<% if @comment.errors.empty? %>
  $('#content').prepend(
    '<%= j render 'shared/flash', flash: tr(:comment, 'created') %>'
  );
  $('#new_comment #comment_body').val('');
  $('#new_comment').hide();
  $('#new_comment .comment-errors').hide();
  $('.add-comment').show();


  <% publish_to "/questions/#{@question.id}/comments" do %>
    $('<%= class_with_id(@commentable) %> .comments').append(
      '<%= j render 'comments/comment', comment: @comment,
        commentable: @commentable %>'
    );
    $('.comments .comment').last().css('background-color', '#fffff7');
    $('<%= class_with_id(@comment) %>').find('.controls').hide();
    if ($("header .user-name:contains('<%= @comment.user.name %>')").length) {
      $('<%= class_with_id(@comment) %>').find('.controls').show();
    }
  <% end %>

<% else %>
  $('<%= class_with_id(@commentable) %> #new_comment').html(
    '<%= j render 'comments/form', edit: true, comment: @comment,
      commentable: @commentable %>'
  );
<% end %>